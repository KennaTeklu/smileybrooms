import { NextResponse } from "next/server"

export async function POST(req: Request) {
  // Empty template - functionality removed
  return NextResponse.json({ status: "Template only - no functionality" })
}
